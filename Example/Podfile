use_frameworks!
platform :ios, '8.0'

target 'NumberSpellOutFormatter_Example' do
  pod 'NumberSpellOutFormatter', :path => '../'

  target 'NumberSpellOutFormatter_Tests' do
    inherit! :search_paths
  end
end
